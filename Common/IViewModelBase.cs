namespace Envelopes.Common {

    /// <summary>
    /// Base class for our view models. Added for so we can bind ViewModel to View in our Presenter class.
    /// </summary>
    /// TODO: REVISIT REMOVING THIS EMPTY CLASS
    public interface IViewModelBase {
    }
}